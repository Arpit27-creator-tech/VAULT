// ============================================================
// V.A.U.L.T — Operative ID Card Customization & Themes
// Perfectly tailored to V.A.U.L.T's Deep Forest / Emerald / Amber aesthetic
// ============================================================

export const CARD_THEMES = {
  CANOPY_EMERALD: {
    id: 'CANOPY_EMERALD',
    name: 'Canopy Operative (Default)',
    subtitle: 'Signature V.A.U.L.T Deep Forest & Emerald Foil',
    primaryColor: '#10B981',
    accentColor: '#34D399',
    goldColor: '#FBBF24',
    borderColor: 'rgba(16, 185, 129, 0.5)',
    bgGradient: 'from-[#072518] via-[#051C12] to-[#020B06]',
    chipColor: '#FBBF24',
    badgeBg: 'bg-[#10B981]/20 text-[#34D399] border-[#10B981]/40',
    holoGradient: 'linear-gradient(135deg, rgba(16,185,129,0.28) 0%, rgba(52,211,153,0.12) 25%, rgba(251,191,36,0.2) 50%, rgba(16,185,129,0.25) 100%)',
    statGlow: 'rgba(16, 185, 129, 0.25)',
    previewSwatch: 'bg-gradient-to-br from-[#10B981] via-[#064e3b] to-[#022c22]'
  },
  VAULT_GOLD: {
    id: 'VAULT_GOLD',
    name: 'Syndicate High-Roller',
    subtitle: 'Gilded Amber & Deep Obsidian Forest',
    primaryColor: '#FBBF24',
    accentColor: '#FDE047',
    goldColor: '#FBBF24',
    borderColor: 'rgba(251, 191, 36, 0.6)',
    bgGradient: 'from-[#1f1604] via-[#0f1710] to-[#020b06]',
    chipColor: '#FBBF24',
    badgeBg: 'bg-[#FBBF24]/20 text-[#FDE047] border-[#FBBF24]/50',
    holoGradient: 'linear-gradient(135deg, rgba(251,191,36,0.35) 0%, rgba(245,158,11,0.18) 35%, rgba(254,240,138,0.3) 70%, rgba(251,191,36,0.35) 100%)',
    statGlow: 'rgba(251, 191, 36, 0.35)',
    previewSwatch: 'bg-gradient-to-br from-[#FBBF24] via-[#B45309] to-[#051C12]'
  },
  CYBER_HACKER: {
    id: 'CYBER_HACKER',
    name: 'Terminal Cryptographer',
    subtitle: 'Bioluminescent Mint & Cyan Security Grid',
    primaryColor: '#06B6D4',
    accentColor: '#34D399',
    goldColor: '#FBBF24',
    borderColor: 'rgba(6, 182, 212, 0.5)',
    bgGradient: 'from-[#032024] via-[#041a14] to-[#020b06]',
    chipColor: '#06B6D4',
    badgeBg: 'bg-cyan-500/20 text-cyan-300 border-cyan-500/40',
    holoGradient: 'linear-gradient(135deg, rgba(6,182,212,0.3) 0%, rgba(16,185,129,0.2) 40%, rgba(34,211,238,0.28) 100%)',
    statGlow: 'rgba(6, 182, 212, 0.3)',
    previewSwatch: 'bg-gradient-to-br from-[#06B6D4] via-[#047857] to-[#020B06]'
  },
  SHADOW_RECON: {
    id: 'SHADOW_RECON',
    name: 'Shadow Infiltrator',
    subtitle: 'Ultra-Matte Night Canopy & Zero-Signature Black',
    primaryColor: '#34D399',
    accentColor: '#94A3B8',
    goldColor: '#FBBF24',
    borderColor: 'rgba(52, 211, 153, 0.35)',
    bgGradient: 'from-[#0a1510] via-[#060e0a] to-[#010402]',
    chipColor: '#94A3B8',
    badgeBg: 'bg-emerald-950/80 text-slate-300 border-emerald-800/40',
    holoGradient: 'linear-gradient(135deg, rgba(52,211,153,0.15) 0%, rgba(148,163,184,0.15) 50%, rgba(16,185,129,0.2) 100%)',
    statGlow: 'rgba(16, 185, 129, 0.15)',
    previewSwatch: 'bg-gradient-to-br from-[#1E293B] via-[#062016] to-[#010402]'
  },
  SOLAR_OVERCLOCK: {
    id: 'SOLAR_OVERCLOCK',
    name: 'Laser Grid Engineer',
    subtitle: 'Overcharged Solar Pulse & Deep Moss Green',
    primaryColor: '#EAB308',
    accentColor: '#84CC16',
    goldColor: '#FBBF24',
    borderColor: 'rgba(234, 179, 8, 0.5)',
    bgGradient: 'from-[#191d05] via-[#071f14] to-[#020b06]',
    chipColor: '#EAB308',
    badgeBg: 'bg-yellow-500/20 text-yellow-300 border-yellow-500/40',
    holoGradient: 'linear-gradient(135deg, rgba(234,179,8,0.3) 0%, rgba(132,204,22,0.2) 40%, rgba(251,191,36,0.3) 100%)',
    statGlow: 'rgba(234, 179, 8, 0.3)',
    previewSwatch: 'bg-gradient-to-br from-[#EAB308] via-[#15803D] to-[#020B06]'
  },
  HIGH_ALERT_CRIMSON: {
    id: 'HIGH_ALERT_CRIMSON',
    name: 'Demolitions Breacher',
    subtitle: 'Emergency Alert Crimson & Forest Smoke',
    primaryColor: '#EF4444',
    accentColor: '#F97316',
    goldColor: '#FBBF24',
    borderColor: 'rgba(239, 68, 68, 0.5)',
    bgGradient: 'from-[#210909] via-[#0d160f] to-[#020b06]',
    chipColor: '#F97316',
    badgeBg: 'bg-red-500/20 text-red-300 border-red-500/40',
    holoGradient: 'linear-gradient(135deg, rgba(239,68,68,0.3) 0%, rgba(249,115,22,0.2) 40%, rgba(239,68,68,0.3) 100%)',
    statGlow: 'rgba(239, 68, 68, 0.3)',
    previewSwatch: 'bg-gradient-to-br from-[#EF4444] via-[#064e3b] to-[#020B06]'
  }
};

export const FRAME_STYLES = {
  forestCard: {
    id: 'forestCard',
    name: 'Forest Syndicate (Default)',
    description: 'Crisp emerald border with tactile 3px shadow matching V.A.U.L.T interface'
  },
  holographic: {
    id: 'holographic',
    name: 'Holo Security Foil',
    description: 'Dynamic prismatic canopy shimmer with micro-security watermark'
  },
  carbon: {
    id: 'carbon',
    name: 'Tactical Carbon Mesh',
    description: 'Reinforced ultra-light carbon weave with cybernetic corner brackets'
  },
  circuit: {
    id: 'circuit',
    name: 'Neural Circuit Traces',
    description: 'Integrated motherboard tracks reflecting golden laser relay pulses'
  }
};

export const PRESET_MOTTOS = [
  'Apex Infiltrator // Zero Trace',
  'Never Leave a Partner Behind',
  'Keymaster of the Canopy Vault',
  'Ghost of the Syndicate',
  'Laser Grid Specialist',
  'Mastermind Locksmith',
  'Clutch Operator Under Pressure',
  'Silent Infiltration Expert'
];

export const AVAILABLE_SHOWCASE_MEDALS = [
  { id: 'mvp_award', title: 'Operation MVP', icon: 'Trophy', tier: 'GOLD', desc: 'Awarded for supreme MVP performance in syndicate heists.' },
  { id: 'first_breach', title: 'First Infiltration', icon: 'CheckCircle2', tier: 'BRONZE', desc: 'Successfully breached and cracked an operation chamber.' },
  { id: 'speed_demon', title: 'Sub-Zero Velocity', icon: 'Clock', tier: 'SILVER', desc: 'Completed a heist operation in record sub-2-minute speed.' },
  { id: 'apex_loyalty', title: 'Squad Vanguard', icon: 'Shield', tier: 'PLATINUM', desc: 'Maintains elite 1,000 Loyalty Point standing with squad.' },
  { id: 'ghost_protocol', title: 'Ghost Protocol', icon: 'Sparkles', tier: 'GOLD', desc: 'Cracked secure vaults with zero alarms or triggers tripped.' },
  { id: 'neural_hacker', title: 'Cybernetic Breaker', icon: 'Terminal', tier: 'PLATINUM', desc: 'Mastery of advanced terminal cryptography and relays.' },
  { id: 'clutch_save', title: 'Clutch Specialist', icon: 'Zap', tier: 'GOLD', desc: 'Saved squad extraction with under 10 seconds on clock.' }
];

export const DEFAULT_CARD_CONFIG = {
  theme: 'CANOPY_EMERALD',
  frameStyle: 'forestCard',
  avatarShape: 'circle', // 'circle' | 'roundedRect'
  motto: 'Apex Infiltrator // Zero Trace',
  showcasedMedals: ['mvp_award', 'speed_demon', 'apex_loyalty'],
  hologramShimmer: true,
  showBarcode: true
};

/**
 * Retrieve or compute the card configuration for an operative.
 */
export function getOperativeCardConfig(user) {
  if (!user) return DEFAULT_CARD_CONFIG;

  // 1. Check embedded cardConfig in user object
  if (user.cardConfig && typeof user.cardConfig === 'object') {
    const rawTheme = user.cardConfig.theme;
    // Remap any obsolete legacy themes to valid V.A.U.L.T themes
    const validTheme = CARD_THEMES[rawTheme] ? rawTheme : 'CANOPY_EMERALD';
    return {
      ...DEFAULT_CARD_CONFIG,
      ...user.cardConfig,
      theme: validTheme
    };
  }

  // 2. Check localStorage cache for saved agent card customizations
  try {
    const raw = localStorage.getItem('vault_card_customizations');
    if (raw) {
      const map = JSON.parse(raw);
      const key = user.agentId || user.id || user.callsign;
      if (key && map[key]) {
        const savedTheme = map[key].theme;
        const validTheme = CARD_THEMES[savedTheme] ? savedTheme : 'CANOPY_EMERALD';
        return {
          ...DEFAULT_CARD_CONFIG,
          ...map[key],
          theme: validTheme
        };
      }
    }
  } catch (e) {
    // Ignore parse errors
  }

  // 3. Fallback: choose thematic defaults based on operative role
  let theme = 'CANOPY_EMERALD';
  const role = (user.role || '').toLowerCase();
  if (role.includes('hacker') || role.includes('crypto')) theme = 'CYBER_HACKER';
  else if (role.includes('demolition') || role.includes('combat')) theme = 'HIGH_ALERT_CRIMSON';
  else if (role.includes('infiltrator') || role.includes('ghost')) theme = 'SHADOW_RECON';
  else if (role.includes('engineer')) theme = 'SOLAR_OVERCLOCK';
  else if ((user.level || 1) >= 4) theme = 'VAULT_GOLD';

  return {
    ...DEFAULT_CARD_CONFIG,
    theme,
    motto: user.rank ? `Rank: ${user.rank}` : DEFAULT_CARD_CONFIG.motto
  };
}

/**
 * Save user card configuration locally and dispatch update event.
 */
export function saveOperativeCardConfig(user, config) {
  if (!user) return;
  const merged = { ...DEFAULT_CARD_CONFIG, ...config };

  // Update in localStorage cache
  try {
    const raw = localStorage.getItem('vault_card_customizations') || '{}';
    const map = JSON.parse(raw);
    const key = user.agentId || user.id || user.callsign;
    if (key) {
      map[key] = merged;
      localStorage.setItem('vault_card_customizations', JSON.stringify(map));
    }

    // Also update currentUser in localStorage if applicable
    const curRaw = localStorage.getItem('vault_current_user');
    if (curRaw) {
      const curUser = JSON.parse(curRaw);
      curUser.cardConfig = merged;
      localStorage.setItem('vault_current_user', JSON.stringify(curUser));
    }
  } catch (e) {
    console.error('[CARD CONFIG] Error persisting card settings:', e);
  }

  try {
    window.dispatchEvent(new CustomEvent('vault:user-updated', { detail: { cardConfig: merged } }));
    window.dispatchEvent(new CustomEvent('vault:card-updated', { detail: { userId: user.id, cardConfig: merged } }));
  } catch (e) {}

  return merged;
}
